<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:background="@drawable/dialog_bg_detail" android:layout_width="wrap_content"
	android:layout_height="wrap_content" android:layout_marginLeft="10dip"
	android:layout_marginRight="10dip" android:layout_marginTop="40dip"
	android:layout_marginBottom="40dip">
	<!-- -->
	<TextView android:id="@+id/tv_title_tip" android:layout_width="fill_parent"
		android:layout_height="wrap_content" android:layout_marginTop="16dip"
		android:textSize="16dip" 
		android:text="您当前的位置:"
		android:layout_marginLeft="80dip" />
	<TextView android:id="@+id/tv_title" android:layout_below="@id/tv_title_tip" android:layout_width="fill_parent"
		android:layout_height="wrap_content" 
		android:textSize="16dip" android:textColor="#cb0c0c"
		android:layout_marginLeft="80dip" />
	<!-- -->
	<ImageButton android:id="@+id/btn_cancel" android:padding="6dip"
		android:layout_width="80dip" android:layout_height="wrap_content"
		android:layout_alignParentBottom="true" android:scaleType="centerInside"
		android:layout_centerHorizontal="true" android:src="@drawable/btn_cancel"
		android:background="@drawable/btn_sel_background" />
	<!-- -->
	<ListView android:id="@+id/list_view" android:layout_margin="10dip"
		android:scrollbarThumbVertical="@drawable/scroll_bar"
		android:layout_below="@id/tv_title" android:layout_above="@id/btn_cancel"
		android:layout_width="fill_parent" android:listSelector="@drawable/list_item_background_0"
		android:divider="#00000000" android:layout_height="fill_parent"
		android:background="@drawable/dialog_list_bg2" />
	<!-- -->
	<ProgressBar android:id="@+id/progressBar"
		android:layout_centerInParent="true"
		style="@android:style/Widget.ProgressBar.Inverse" mce_style="@android:style/Widget.ProgressBar.Inverse"
		android:layout_width="wrap_content" android:layout_height="wrap_content" />
</RelativeLayout>